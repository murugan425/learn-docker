ps aux | grep

ls -a

ls -al

apt-get update 

apt-get install curl 

apt-get install -y procps <if 'ps aux' doesn't runs>

yum update curl

ifconfig -en0

apt-get install build-essential procps curl file git

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

alias cls='printf "\033c"'

-----------------------------------------------------------------------------------------
ACCESS the registry to get the required account info, 
-----------------------------------------------------------------------------------------
TOKEN=$(curl "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | grep -r .token)

TOKEN=$(curl --user 'username:password' "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | grep -r .token)

curl --head -H "Authorization:<SPACE>Bearer $TOKEN" https://registry-1.docker.io/v2/ratelimitpreview/test/manifests/latest

TOKEN=$(curl --user 'murugan425:jayasri425' "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | jq -r .token)

curl --user 'murugan425:ee4e3e9d-73c7-4381-b330-4654b7d7f237' "https://auth.docker.io/token?service=registry.docker.io&scope=repository:ratelimitpreview/test:pull" | grep -r .token
